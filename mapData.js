import express from 'express'
import bodyParser from 'body-parser'

const app = express()


app.get(API/history, (req,res) => {
    const lat = res.params.lat;
    const lang = res.params.lng;
    const company = res.params.lng;
    const 
})
    .then(res => res.json())
    .then()